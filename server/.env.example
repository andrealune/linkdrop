# Server-specific overrides (optional).
# The authoritative list of variables lives in the repo root .env.example.
# Copy the root file to `.env` at the repo root; the server reads it via
# dotenv when run from the `server/` directory with `npm run dev`.
